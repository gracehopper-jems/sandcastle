import React from 'react'; 


export const DatabaseTable = (props) => {

	console.log("DATABASE TABLE PROPS", props)

}

          {/* props.docker.database.map(obj => {
              return (<p key={obj.slice(6, 8)}>{obj.toString()}</p>)
            }) */} 